# DEEBOT Y1 PRO upstream patch workspace

This directory mirrors the files intended for a future `DeebotUniverse/client.py` pull request. It is isolated from the working Home Assistant add-on and the known-good Diagnostics 2.0.41 / profile 1.8.18 runtime.

Initial scope: core `cqyi87` discovery, numeric cleaning controls, `10000` partial state parsing, and `10001` field queries. Map support remains a follow-up patch so the first upstream change stays reviewable.
